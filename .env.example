# Environment variables for Scholarport Backend
# Copy this file to .env and fill in your actual values

# Django settings
SECRET_KEY=your-secret-key-here-make-it-long-and-random
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database configuration (SQLite for development, PostgreSQL for production)
DATABASE_URL=sqlite:///db.sqlite3

# OpenAI API configuration
# ============================================
# Scholarport Backend - Environment Variables
# ============================================
# Copy this file to .env and fill in your values

# ============================================
# Django Settings
# ============================================
DJANGO_ENV=development
DEBUG=True
SECRET_KEY=your-secret-key-change-this-in-production-min-50-chars
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================
# Database Configuration
# ============================================
# Development uses SQLite by default
# Production uses PostgreSQL (see .env.production)

# ============================================
# API Keys & External Services
# ============================================
# OpenAI API Key (Required)
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# Firebase Configuration (Required)
FIREBASE_CREDENTIALS_PATH=scholorport-firebase-adminsdk-fbsvc-b17f9acfbf.json

# ============================================
# CORS Settings (for frontend integration)
# ============================================
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ============================================
# Email Configuration (Optional - for future features)
# ============================================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Firebase configuration
FIREBASE_CREDENTIALS_PATH=path/to/your/firebase/credentials.json

# CORS settings for frontend integration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Production settings (set these when deploying)
# DATABASE_URL=postgresql://user:password@host:port/database
# DEBUG=False
# ALLOWED_HOSTS=api.scholarport.com